# Telegram bots
python-telegram-bot[job-queue]>=20.0
apscheduler>=3.10

# LLM providers (install the ones you need)
google-generativeai>=0.5.0   # legacy SDK — still used by some tools
google-genai>=1.0.0           # new SDK — used by voice.py and newer bots
# anthropic>=0.25.0           # uncomment if using Claude provider
# openai>=1.0.0               # uncomment if using OpenAI provider

# Vector DB (brain)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Vault sync
gitpython>=3.1.0

# HTTP
requests>=2.31.0

# Scheduling
isoweek>=1.3.3

# Utilities
python-dotenv>=1.0.0
